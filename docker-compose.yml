###############################################################################
#                          Generated on phpdocker.io                          #
###############################################################################

services:
    redis:
        image: 'redis:alpine'

    mysql:
        image: 'mysql:8.0'
        container_name: phpdocker-mysql
        working_dir: /application
        volumes:
            - '.:/application'
            - 'mysql_data:/var/lib/mysql'
        environment:
            - MYSQL_ROOT_PASSWORD=toor
            - MYSQL_DATABASE=parishmanager
            - MYSQL_USER=parishmanager
            - MYSQL_PASSWORD=secret
        ports:
            - '52002:3306'

    webserver:
        image: 'nginx:alpine'
        container_name: phpdocker-nginx
        working_dir: /application
        volumes:
            - './app:/application'
            - './phpdocker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf'
        ports:
            - '52000:80'

    php-fpm:
        build: phpdocker/php-fpm
        container_name: phpdocker-php-fpm
        user: "${UID:-1000}:${GID:-1000}"
        working_dir: /application
        volumes:
            - './app:/application'
            - './phpdocker/php-fpm/php-ini-overrides.ini:/etc/php/8.5/fpm/conf.d/99-overrides.ini'
            - './phpdocker/php-fpm/php-ini-overrides.ini:/etc/php/8.5/cli/conf.d/99-overrides.ini'

    phpmyadmin:
        image: phpmyadmin/phpmyadmin
        container_name: phpdocker-phpmyadmin
        depends_on:
            - mysql
        environment:
            PMA_HOST: mysql
            PMA_PORT: 3306
            MYSQL_ROOT_PASSWORD: root
        ports:
            - "52001:80"

volumes:
    mysql_data:
